amends "AppConfig.pkl"

port = "port"

db {
  dbName = "dbname"
  host = "host"
  password = "password"
  user = "user"
  port = "port"
}

cloudflare {
  accountID = "id"
  kvNamespaceID = "id"
  token = "token"
  turnstileSecret = "secret"
}

smtp {
  host = "host"
  port = "port"
  user = "user"
  password = "password"
}

auth {
  domain = "domain"
  audience = "audience"
}
