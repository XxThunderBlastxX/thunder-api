amends "AppConfig.pkl"

port = "port"

db {
  dsn = "dsn"
}

cloudflare {
  accountID = "id"
  kvNamespaceID = "id"
  token = "token"
  turnstileSecret = "secret"
}

auth {
  domain = "domain"
  audience = "audience"
}
